# نسخه‌ی ارتقایافته

این نسخه روی پروژه‌ی قبلی ساخته شده و دیتابیس‌های قبلی را با migration به schema 4 می‌آورد.

## شروع سریع

```bash
cp .env.example .env
# TELEGRAM_BOT_TOKEN و ADMIN_USER_IDS را تنظیم کن
pip install -r requirements.txt
python main.py
```

بعد از `/start` از `/admin` این موارد را تنظیم کن:

1. در **مدل‌های AI** مدل و API URL/Key را اضافه و تست کن.
2. در **تنظیمات** حالت سهمیه را روی `messages`، `tokens` یا `both` بگذار.
3. سقف توکن رایگان، سرعت نمایش تدریجی، سقف خروجی و حجم فایل را تنظیم کن.
4. در **فروشگاه** برای هر محصول تعداد پیام، تعداد توکن و مدل‌های مجاز را تعیین کن.
6. از **⚡ قابلیت‌ها** در پنل ادمین، هر ۵۰ قابلیت را مستقل تنظیم کن. این صفحه سه محدوده دارد: سراسری، محصول و کاربر/اشتراک. قابلیت‌های عددی مثل حجم فایل، تعداد فایل ZIP، طول متن استخراج‌شده و عمق حافظه preset چرخشی دارند.
7. سیاست قابلیت محصول هنگام فعال‌سازی اشتراک کپی می‌شود؛ بنابراین ویرایش محصول روی مشتریان قدیمی اثر ناخواسته نمی‌گذارد.

## امکانات دکمه‌ای

- منوی اصلی: چت، ابزارهای سریع، مصرف، فروشگاه، حساب و پشتیبانی
- ابزارهای سریع برای خلاصه‌سازی، ترجمه، بازنویسی، کدنویسی و تحلیل رسید
- ۲۰ ابزار دکمه‌ای در سه صفحه: فکر عمیق، جست‌وجوی وب، تحقیق چندمنبعی، راستی‌آزمایی، تحلیل لینک، مقایسه، ایمیل، رزومه، تبلیغات، JSON، bullet، جدول، ماشین‌حساب، تاریخ/ساعت و خروجی چت
- جست‌وجوی وب بدون API Key با نمایش منابع و citation در prompt مدل
- انتخاب سبک پاسخ: کوتاه، متعادل، کامل و رسمی
- انتخاب حالت سهمیه و presetهای پیام/توکن از پنل ادمین بدون تایپ
- تنظیم سیاست هر کاربر و هر محصول با دکمه و انتخاب مدل مجاز


## ورودی‌های پشتیبانی‌شده

- متن معمولی و caption عکس
- عکس برای مدل‌های vision (به‌صورت data URL امن)
- PDF و DOCX با استخراج متن
- XLSX با استخراج سلول‌ها به متن
- ZIP در حافظه، با جلوگیری از path traversal و محدودیت تعداد فایل/متن استخراج‌شده/حجم بازشده
- رسید خرید همچنان جداگانه برای صف تأیید مدیر ارسال می‌شود.

اگر provider توکن مصرف‌شده را برگرداند، همان مقدار ذخیره می‌شود؛ در غیر این صورت ربات مقدار محافظه‌کارانه‌ی تخمینی را با علامت `~` نشان می‌دهد.
